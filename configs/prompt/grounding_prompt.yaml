# configs/prompt/grounding_prompt.yaml

## RefCOCO 계열 및 grounding 태스크용 기본 템플릿
# Qwen3-VL 스타일의 <image> 플레이스홀더 사용

system_prompt: |
  

user_prompt: |
  <image>
  Locate every object(person, light, etc.) in the image. Output as a JSON array of objects, each with "bbox_2d": [x1, y1, x2, y2] (in 0-1000 normalized coordinates).

